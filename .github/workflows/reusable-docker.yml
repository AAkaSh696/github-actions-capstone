name: Docker Build

on:
  workflow_call:
    inputs:
      image_name:
        required: true
        type: string
      tag:
        required: true
        type: string
    secrets:
      docker_username:
        required: true
      docker_token:
        required: true
    outputs:
      image_url:
        value: ${{ jobs.export.outputs.image }}

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.meta.outputs.image }}

    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - id: meta
        run: |
          IMAGE="${{ secrets.docker_username }}/${{ inputs.image_name }}:${{ inputs.tag }}"
          echo "image=$IMAGE" >> $GITHUB_OUTPUT
          echo "DEBUG IMAGE = $IMAGE"

      - uses: docker/build-push-action@v7
        with:
          push: true
          context: .
          tags: ${{ steps.meta.outputs.image }}

  # 🔥 NEW JOB (THIS FIXES EVERYTHING)
  export:
    runs-on: ubuntu-latest
    needs: build
    outputs:
      image: ${{ needs.build.outputs.image }}

    steps:
      - name: Pass output forward
        run: echo "Image passed = ${{ needs.build.outputs.image }}"